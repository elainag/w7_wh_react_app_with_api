import React from "react";

const DriverDetails = ({ driver }) => {
    // const details = driver
    return (
        <div>
            {/* <h3>{index + 1}</h3> */}
            <p>{driver.driverid}</p>
            <p>{driver.driverid}</p>
            <p>{driver.driverid}</p>
            <p>{driver.driverid}</p>
            <p>{driver.driverid}</p>
        </div>

    )

}

export default DriverDetails;